<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 刻意不声明 INTERNET 权限：纯离线是本应用的核心承诺。
         gomobile 生成的 AAR manifest 亦不含任何权限（见 x/mobile bind_androidapp.go）。 -->
    <!-- Android 12 及以下访问共享存储 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- Android 11+ 全盘文件访问（文件搜索属官方许可场景） -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AnythingMobile"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 崩溃报告页：独立进程运行，主进程 OOM 崩溃时也能展示/分享日志 -->
        <activity
            android:name=".crash.CrashReportActivity"
            android:exported="false"
            android:process=":crash" />

        <!-- 下拉快捷设置磁贴：一键进入搜索（对标原版 QuickStartTileService） -->
        <service
            android:name=".tile.QuickSearchTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_search"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

        <!-- 文件分享（对标原版 share_extend） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
